## **1. Executive Status Report & Master Plan**

### **Milestone 1: Portfolio MVP — 100% COMPLETED**

All core stabilization blockers, checkout address serialization, static vector SVG asset integrations, and backend/frontend Admin Dashboard live KPI bindings have been merged and verified. The application is fully demo-ready for basic browsing, cart management, and order placement.

### **Milestone 2: Complete Lifecycle & Admin Polish — NEXT UP**

This milestone focuses on closing the end-to-end e-commerce transaction lifecycle (`Pending` $\rightarrow$ `Processing` $\rightarrow$ `Shipped` $\rightarrow$ `Delivered`) and replacing the three grayed-out "Coming Soon" left-navigation links (`Customers`, `Reports`, `Admin...`) with functional administrative views.

### **Master Plan Execution Table**

| Milestone | Task ID | Feature / Component | Status | Next Action |
| --- | --- | --- | --- | --- |
| **Milestone 1** | `MVP-001` | Checkout Shipping Address Serialization | **COMPLETED** | Merged & verified.

 |
| **Milestone 1** | `MVP-004` | Catalog Inline SVG Fallback | **COMPLETED** | Merged & verified.

 |
| **Milestone 1** | `MVP-005` | SVG Catalog Assets & 170px Image Box Resizing | **COMPLETED** | Merged & verified.

 |
| **Milestone 1** | `MVP-002` | `/api/v1/admin/dashboard` FastAPI Router | **COMPLETED** | Merged & verified (`dashboard_router.py`).

 |
| **Milestone 1** | `MVP-003` | Angular `DashboardService` Live Binding | **COMPLETED** | Verified via audit.

 |
| **Milestone 2** | `ENT-001` | Sandbox Payment Gateway & Lite User Profile | **NEXT UP** | **Milestone 2 Kickoff Task.** |
| **Milestone 2** | `ENT-002` | Admin Order Fulfillment & Status Manager (`Admin...`) | **BACKLOG** | Scheduled for Milestone 2. |
| **Milestone 2** | `ENT-003` | Customer Directory (`Customers` Sidenav) | **BACKLOG** | Scheduled for Milestone 2. |
| **Milestone 2** | `ENT-004` | Operations & Inventory Reports (`Reports` Sidenav) | **BACKLOG** | Scheduled for Milestone 2. |
| **Milestone 2** | `ENT-005` | RBAC Role Normalization (Python Enums) | **BACKLOG** | Scheduled for Milestone 2. |
| **Milestone 3** | `DEBT-001` | Redis Caching Wrappers in `ProductService` | **BACKLOG** | Scheduled for Milestone 3. |
| **Milestone 3** | `DEBT-002` | Fix Component Projection Selectors in Jasmine Suite | **BACKLOG** | Scheduled for Milestone 3. |
| **Milestone 3** | `OPS-001` | Automated CI/CD Build & Lint Workflow with Badge | **BACKLOG** | Scheduled for Milestone 3. |

---

## **2. Open & Backlog GitHub Issues (Milestones 2 & 3)**

You can copy and paste the Markdown blocks below directly into your GitHub repository's issue tracker.

### **Issue 1: `ENT-001` — Sandbox Payment Gateway & Lite User Profile**

```markdown
### **Title**
[Milestone 2] Sandbox Payment Gateway & Lite User Profile (#ENT-001)

### **Labels**
`feature` `full-stack` `payments` `profile` `priority-critical`

### **Objective**
Enable customers to view a lite profile summary and complete simulated payment checkouts in test mode, advancing order status from `Pending` to `Processing`.

### **Technical Specification**
1. **Lite User Profile (`/profile`):**
   - Create a clean Profile view showing account details (Name, Email, Role) and default/saved shipping address metadata.
2. **Backend (FastAPI):**
   - Implement `POST /api/v1/orders/{order_id}/checkout-session` returning a sandbox payment URL or simulated checkout token.
   - Implement `POST /api/v1/orders/{order_id}/confirm-payment` to atomically transition order status from `Pending` to `Processing`.
3. **Frontend (Angular 19):**
   - Add a "Proceed to Payment" action on `Pending` orders in the Order Details view.
   - Handle the payment simulation/redirect and update UI order status reactively.

### **Definition of Done**
- [ ] Users can view their basic profile details on a dedicated `/profile` screen.
- [ ] Customers can initiate and complete a sandbox payment flow on a `Pending` order.
- [ ] Successful payment updates the database order status from `Pending` to `Processing`.

```

---

### **Issue 2: `ENT-002` — Admin Order Fulfillment & Status Manager (`Admin...` Sidenav)**

```markdown
### **Title**
[Milestone 2] Admin Order Fulfillment & Status Manager (#ENT-002)

### **Labels**
`feature` `full-stack` `admin` `orders` `priority-high`

### **Objective**
Replace the grayed-out `Admin...` Sidenav link with an Admin Operations control panel to manage system orders and advance fulfillment lifecycles (`Processing` -> `Shipped` -> `Delivered`).

### **Technical Specification**
1. **Backend (FastAPI):**
   - Implement `GET /api/v1/admin/orders` to fetch all orders across all customers (protected by Admin RBAC).
   - Implement `PATCH /api/v1/admin/orders/{order_id}/status` accepting valid state transitions (`Shipped`, `Delivered`, `Cancelled`).
2. **Frontend (Angular 19):**
   - Replace the `Admin...` Sidenav item with `Admin Operations` linking to `/admin/orders`.
   - Build a data table displaying system orders with a status update action dropdown.

### **Definition of Done**
- [ ] Administrators can view all system orders across all customers.
- [ ] Administrators can advance order statuses through `Processing` -> `Shipped` -> `Delivered`.
- [ ] Non-admin users receive `403 Forbidden` if attempting to access the route or API.

```

---

### **Issue 3: `ENT-003` — Admin Customer Directory (`Customers` Sidenav)**

```markdown
### **Title**
[Milestone 2] Admin Customer Directory (#ENT-003)

### **Labels**
`feature` `full-stack` `admin` `users` `priority-medium`

### **Objective**
Replace the grayed-out `Customers` Sidenav link with a read-only directory table listing registered user accounts.

### **Technical Specification**
1. **Backend (FastAPI):**
   - Implement `GET /api/v1/admin/users` returning a list of registered users with email, role, active status, and registration date.
2. **Frontend (Angular 19):**
   - Replace the `Customers` Sidenav item with an active link to `/admin/customers`.
   - Build an OnPush Angular table rendering customer directory metadata cleanly.

### **Definition of Done**
- [ ] Clicking `Customers` in the Sidenav routes Admin users to the Customer Directory table.
- [ ] Table renders live PostgreSQL user records accurately.

```

---

### **Issue 4: `ENT-004` — Executive Operations & Inventory Reports (`Reports` Sidenav)**

```markdown
### **Title**
[Milestone 2] Executive Operations & Inventory Reports (#ENT-004)

### **Labels**
`feature` `full-stack` `admin` `reports` `priority-medium`

### **Objective**
Replace the grayed-out `Reports` Sidenav link with an executive reporting screen highlighting low-stock inventory alerts and sales summary metrics.

### **Technical Specification**
1. **Backend (FastAPI):**
   - Implement `GET /api/v1/admin/reports/low-stock` returning catalog items where `stock_quantity <= 10`.
2. **Frontend (Angular 19):**
   - Replace the `Reports` Sidenav item with an active link to `/admin/reports`.
   - Display a Low-Stock Alert table and a summary metric card of overall sales volume.

### **Definition of Done**
- [ ] Clicking `Reports` routes Admin users to the executive reporting screen.
- [ ] Products with stock <= 10 appear automatically in the Low-Stock Alert table.

```

---

### **Issue 5: `ENT-005` — RBAC Security: Normalize Role Name Comparisons to Enums**

```markdown
### **Title**
[Milestone 2] RBAC Security: Normalize Role Name Comparisons to Python Enums (#ENT-005)

### **Labels**
`security` `backend` `fastapi` `priority-normal`

### **Objective**
Replace hardcoded, case-sensitive string matching for user roles with normalized Enum comparisons to prevent authorization edge-case failures.

### **Technical Specification**
1. Locate FastAPI role dependency checks (`require_admin`, `require_customer`).
2. Create a Python `Enum` for system roles (`ADMIN = "ADMIN"`, `CUSTOMER = "CUSTOMER"`).
3. Ensure role validation explicitly normalizes database strings (`str.upper()`) against the Role Enum during route authorization.

### **Definition of Done**
- [ ] Backend authorization dependencies evaluate against Enums rather than hardcoded strings.
- [ ] Customer and Admin role-protected endpoints continue to pass all RBAC checks.

```

---

### **Issue 6: `DEBT-001` — Redis Caching Wrappers in `ProductService**`

```markdown
### **Title**
[Milestone 3] Implement Redis Caching Wrappers in `ProductService` (#DEBT-001)

### **Labels**
`performance` `backend` `redis` `priority-normal`

### **Objective**
Implement Redis caching wrappers for frequently queried product catalog endpoints to achieve sub-second response times.

### **Technical Specification**
1. Locate `ProductService.get_products` in the backend service layer.
2. Add Redis `get/set` wrappers to check Redis 8 cache before querying PostgreSQL 17.
3. Set a reasonable TTL (e.g., 300 seconds) and ensure cache invalidation triggers on product mutations.

### **Definition of Done**
- [ ] Repeated calls to `/api/v1/products` serve data directly from Redis cache.
- [ ] Product inventory updates properly invalidate corresponding Redis cache keys.

```

---

### **Issue 7: `DEBT-002` — Fix Component Projection Selectors in Jasmine Suite**

```markdown
### **Title**
[Milestone 3] Fix Component Projection Selectors in Jasmine Test Suite (#DEBT-002)

### **Labels**
`testing` `frontend` `angular19` `priority-normal`

### **Objective**
Resolve the 3 failing unit tests in the frontend Karma/Jasmine test suite (`ErrorStateComponent` and `SearchToolbarComponent`) caused by brittle DOM projection selectors.

### **Technical Specification**
1. Update testbed configurations for `ErrorStateComponent` and `SearchToolbarComponent`.
2. Fix `TypeError: Cannot read properties of null (reading 'nativeElement')` by safely querying projected content slots inside `ViewChild`/`ContentChild` test fixtures.

### **Definition of Done**
- [ ] Running `ng test` executes with 0 failures (134/134 tests passing).

```

---

### **Issue 8: `OPS-001` — Automated CI/CD Build & Lint Workflow with Status Badge**

```markdown
### **Title**
[Milestone 3] Automated CI/CD Build & Lint Pipeline with README Badge (#OPS-001)

### **Labels**
`devops` `ci-cd` `automation` `priority-normal`

### **Objective**
Implement GitHub Actions automation for builds, linting, and regression testing, displaying a passing build badge on the repository README.

### **Technical Specification**
1. Implement `.github/workflows/main.yml` triggered on push and pull requests to `main` and `develop`.
2. **Backend Job:** Set up Python 3.12, install requirements, and execute syntax/linting checks and backend pytest suite.
3. **Frontend Job:** Set up Node.js, install dependencies, and run `ng build` (production build check).
4. Embed the GitHub Actions status badge at the top of `README.md`.

### **Definition of Done**
- [ ] Pushing to the repository automatically triggers green CI pipelines for both backend and frontend.
- [ ] A dynamic, passing build badge renders clearly on the README file.

```

---

## **3. AI Coding Prompt for `ENT-001` (Sandbox Payment Gateway + Lite Profile)**

Copy and paste the block below into your AI coding agent (Jules/Antigravity) to start Milestone 2:

```markdown
# TASK: ENT-001 — Sandbox Payment Gateway Integration & Lite User Profile

## 1. System & Technical Context
- **Backend Stack:** Python 3.12, FastAPI, SQLAlchemy 2.x (Sync), PostgreSQL 17.
- **Frontend Stack:** Angular 19 (Standalone Components, Signals, `OnPush` Change Detection).
- **Current State:** Orders can be placed and persisted with status `Pending`. Shipping addresses are properly serialized.

## 2. Objective
Implement a Lite User Profile view (`/profile`) and integrate a simulated/hosted Sandbox Payment Gateway so customers can view their basic account details and complete a test transaction that transitions order status from `Pending` to `Processing`.

## 3. Backend Requirements (FastAPI)
1. **User Profile Endpoint:**
   - Verify or implement `GET /api/v1/users/me` (or similar profile endpoint) returning the authenticated user's Name, Email, Role, and registration timestamp.
2. **Payment Checkout & Confirmation Endpoints:**
   - Implement `POST /api/v1/orders/{order_id}/checkout-session` protected by Customer RBAC. Ensure it validates that the order belongs to the user and is in `Pending` status.
   - Return a simulated sandbox payment session token or hosted sandbox URL (Stripe Sandbox, Razorpay Test Mode, or a clean self-hosted simulation flow).
   - Implement `POST /api/v1/orders/{order_id}/confirm-payment` to atomically update the database Order status from `Pending` to `Processing` via SQLAlchemy 2.x ORM.

## 4. Frontend Requirements (Angular 19)
1. **Lite Profile Page (`/profile`):**
   - Create a clean Standalone Profile component showing the user's account details (Name, Email, Role) and any saved/default address metadata.
   - Add an accessible navigation link to `/profile` in the top header or user menu.
2. **Order Payment Integration:**
   - Locate the Order Details / Confirmation component (`order-details.component.ts`/`.html`).
   - Add a styled "Proceed to Payment" button visible ONLY when an order's status is `Pending`.
   - Wire the button to trigger `POST /api/v1/orders/{order_id}/checkout-session` and complete the simulated payment flow, automatically updating the UI to reflect the new `Processing` status.

## 5. Strict Execution Guardrails
- **Clean Architecture:** Maintain strict layer separation (`routers` -> `services` -> `repositories`). Do not write raw SQL or legacy `db.query()`.
- **No Regression:** Do not alter existing catalog SVG rendering, currency formatting (`₹`), or order creation payloads.
- **Definition of Done Check:** Confirm that a user can view `/profile`, create an order, click "Proceed to Payment", complete the sandbox flow, and see the database order status change from `Pending` to `Processing`. List all modified/created files in your summary.

```
