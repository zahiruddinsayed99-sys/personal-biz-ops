## Message to Principal Architect
Reviewed and approved.

The proposed **Milestone 9 — Module 4: LMS Engine** blueprint is aligned with the finalized scope we agreed on. I confirm that we should proceed with this specification **as written, without adding further functional scope or architectural changes**.

The intended positioning is correct: LMS should be treated as an **AI-Powered Learning & Enablement module supporting the core CRM**, while remaining loosely coupled for future CRM integrations.

Please proceed with the following deliverables:

1. **Formal Technical Specification**

   * Module boundaries
   * Component responsibilities
   * Service/repository separation
   * RBAC and tenant-isolation enforcement
   * Integration points with existing Billing and AI Platform infrastructure

2. **Detailed Alembic Database Schemas**

   * `courses`
   * `course_modules`
   * `lessons`
   * `course_enrollments`
   * `lesson_progress`
   * Quiz-related entities
   * Quiz attempt persistence
   * Required indexes, foreign keys, constraints, enums, timestamps, and tenant boundaries

3. **Core API Contracts**

   * Course/Module/Lesson authoring APIs
   * Draft/Publish lifecycle
   * Enrollment APIs
   * Learner progress APIs
   * Quiz execution and attempt APIs
   * AI Quiz Generation API
   * Existing asynchronous job-status integration
   * Appropriate authorization and HTTP error contracts

4. **AI Quiz Generation**

   * Define the `lms_quiz_v1` prompt contract
   * Strict structured JSON schema
   * AI Gateway integration
   * Celery execution flow
   * Existing atomic AI-credit deduction mechanism
   * HTTP 402 handling for insufficient credits

5. **Modular GitHub Issues**

   * Break implementation into small, independently executable issues
   * Maintain clear dependencies between issues
   * Include backend, database, AI integration, frontend, testing, and validation work
   * Keep the issues implementation-focused and consistent with the existing repository structure

### Architectural Constraints

The implementation must continue to use the existing BusinessHub architecture:

* Existing Multi-Tenancy infrastructure
* Existing RBAC/permission system
* Existing Billing and Soft-Lock mechanisms
* Existing AI Gateway
* Existing Celery workers
* Existing asynchronous job-status pattern
* Existing repository/service architecture
* Existing API conventions
* Existing testing and migration conventions

**No new baseline architecture should be introduced.**

The LMS must enforce `organization_id` isolation consistently across all tenant-owned data and must not create any cross-tenant access path.

### Scope Freeze

The following remain explicitly out of scope for this MVP and should not appear in the architecture, schema, API contracts, or GitHub issues:

* Certificates
* Gamification / Badges
* Discussion boards / Forums
* SCORM
* Live classes / Webinars
* Advanced learning analytics
* Full external customer-training workflows

The **Course → Module → Lesson**, enrollment, progress, course completion, quiz attempts, and AI Quiz Generator functionality defined in the request represents the complete MVP scope.

Please proceed with the detailed **Technical Specification → Database Schema → API Contracts → GitHub Issues** based strictly on this finalized scope.

No further scope expansion is required at this stage.

---
## Scope
I reviewed the proposed **Module 4: LMS Engine** scope in the context of the overall **Business Hub Fullstack** architecture, with CRM remaining the major/core module.

I agree that the LMS scope aligns well with the Business Hub vision, provided we position it as an **AI-powered Learning & Enablement module**, rather than as a standalone generic LMS.

I am happy to finalize the scope with the following additions/clarifications:

### Confirmed LMS Scope

* Course Builder with **Course → Module → Lesson** hierarchy
* Draft → Published workflow
* Rich-text lesson content and external video links
* RBAC enforcement through `lms:write`
* AI Quiz Generator using the existing **AI Platform, AI Gateway, Celery workers, and credit metering**
* Structured AI quiz JSON using `lms_quiz_v1`
* Async quiz generation with job-status tracking
* AI Credit deduction and graceful insufficient-credit handling
* Course enrollment
* Lesson-level progress tracking
* Quiz execution with immediate feedback and explanations
* **Quiz Attempts** persistence
* **Course Completion** state (`NOT_STARTED`, `IN_PROGRESS`, `COMPLETED`)
* Basic **Course Metadata** such as title, description, category, difficulty, duration, etc.
* Billing/Tier limits using the existing Billing Engine and platform soft-lock mechanism
* Strict tenant isolation using `organization_id`

### Architectural Direction

The LMS should reuse existing Business Hub platform capabilities rather than introduce separate infrastructure:

* Existing RBAC
* Existing tenant isolation
* Existing Billing/Credit system
* Existing AI Gateway
* Existing Celery infrastructure
* Existing asynchronous job-status pattern

CRM integration should be **supported architecturally but not overbuilt in this MVP**. The LMS should remain loosely coupled so that future employee/customer enablement scenarios can connect naturally with CRM.

### Explicitly Out of Scope for This Release

To keep the project on schedule, I do not want to expand the current scope with:

* Certificates
* Gamification
* Discussion/forums
* SCORM
* Live classes
* Advanced learning analytics
* Full customer-training workflows

With these clarifications, I consider the **Module 4 LMS Engine functional scope finalized** and ready for the next planning step: database schema definition, API boundaries, and GitHub issue breakdown.

Please incorporate these points into the master plan without further scope expansion.
