Here is the complete, production-grade handoff prompt for **Milestone 3 (Authentication, RBAC, and Tenant Isolation Middleware)**. 

This prompt is meticulously designed to enforce our asymmetric **RS256 cryptosystem**, stateful **Redis sessions**, strict **tenant-isolation middleware**, and highly responsive **RBAC permission caching**. It guides your AI coding agent (Jules / Antigravity) to build out the core security nervous system of the SaaS platform while fully respecting the pre-built local containers.

***

### 📋 Copy-Paste Prompt for Jules / Antigravity

```markdown
USER_REQUEST:
We have successfully implemented and verified the database DDL models and async Alembic migrations (Milestone 2) on 'develop'.

Your goal for Milestone 3 is to implement the Core Security Framework: RS256 JWT Authentication, Stateful Redis Session Management, the Multi-Tenant Isolation Middleware, and the Role-Based Access Control (RBAC) verification engine with Redis-cached permissions.

Please execute the following tasks:

1. GIT BRANCH & WORKFLOW
- Create and switch to a new local feature branch off 'develop' named exactly: track/3-auth-tenant
- Follow strict Human-in-the-Loop guidelines: Do NOT attempt to push directly to 'main' or 'develop'. All deliverables must be prepared for a Draft Pull Request.
- All commit messages must follow the Conventional Commits specification (e.g., "feat(auth): implement rs256 authentication and tenant middleware").

2. RS256 AUTHENTICATION UTILITIES (Asymmetric Cryptosystem)
- Implement JWT encoding and decoding using RS256 (asymmetric signing).
- In `backend/app/core/config.py`, extend Settings to load RSA private and public keys from the environment variables (JWT_PRIVATE_KEY, JWT_PUBLIC_KEY).
- Implement a fallback mechanism for the local development environment: if keys are not present in `.env`, dynamically generate a temporary, valid RS256 key pair on startup and issue a clear startup warning (never commit generated keys to the repository).
- Access Tokens: Set to expire in 15 minutes. Include user_id, email, and active roles in the claims payload.
- Refresh Tokens: Set to expire in 7 days. These must be served strictly via secure, signed, HttpOnly, SameSite=Strict cookies to the client.

3. STATEFUL REDIS SESSIONS
- Upon successful login, write the session record to your running Redis cache.
- The session key must strictly follow the system contract pattern: "sess:{user_id}:{token_id}" with a 7-day TTL.
- Implement token revocation (blacklist/session kill) in Redis upon user logout or password changes.
- Check this Redis session store during token validation to prevent the use of revoked access/refresh tokens.

4. MULTI-TENANT ISOLATION MIDDLEWARE (tenant_middleware.py)
- In `backend/app/core/tenant_middleware.py`, write a FastAPI middleware or dependency layer that intercepts all incoming non-public API endpoints:
  * Extract the JWT and validate the active session in Redis.
  * Extract the mandatory 'X-Organization-Id' header from the request.
  * Validate that the authenticated user belongs to the requested organization_id (by checking the 'user_roles' table).
- If the JWT is invalid or missing: Abort the request and return an HTTP 401 with error code 'ERR_AUTH_001'.
- If the 'X-Organization-Id' header is missing, or if the user is not a member of that organization: Abort the request and return an HTTP 403 with error code 'ERR_TENANT_001'.
- Securely bind the active validated organization_id to the database session's query context to guarantee row-level data isolation.

5. CACHED ROLE-BASED ACCESS CONTROL (RBAC)
- Implement a declarative dependency wrapper (e.g., `RequiresPermission("crm:write")`) that verifies user permissions against our system RBAC matrix.
- To keep validation times under 200 ms, cache the active user permissions in Redis on the key pattern: "org:{org_id}:usr:{user_id}:perms" with a 15-minute TTL.
- Automatically evict this cache key if a user's role is updated or deactivated in that organization.
- If a user lacks the required scope for an operation, return an HTTP 403 with error code 'ERR_RBAC_001'.

6. INTEGRATION TESTING GATES
- Write comprehensive tests in `backend/tests/`:
  * Verify that User A from Organization 1 receives 'ERR_TENANT_001' (403) when attempting to pass Organization 2's ID.
  * Verify that expired or tampered RS256 tokens return 'ERR_AUTH_001' (401).
  * Verify that a user lacking a specific permission scope returns 'ERR_RBAC_001' (403).
  * Mock or utilize the local Redis container to verify session keys ("sess:...") and permission keys ("org:...") are successfully set, read, and evicted.

When complete, verify all tests pass locally (`pytest -v`), commit your work to 'track/3-auth-tenant', and submit a Draft Pull Request for architectural review.
```

***

🔒 Once Jules submits the Draft PR for this security layer, we can perform a deep review of the RS256 token verification logic, the middleware error boundaries, and the Redis cache eviction triggers. Let me know when you're ready to proceed!
