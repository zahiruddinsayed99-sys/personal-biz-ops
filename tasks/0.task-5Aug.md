Here are the step-by-step instructions to verify the database models and Alembic migrations on your local WSL environment:

---

### Step 1: Ensure Local Docker Infrastructure is Running

Make sure the PostgreSQL container is active:

```bash
cd ~/projects/businesshub-ai
docker compose up -d
```

Verify the `businesshub_postgres` container is healthy:
```bash
docker ps
```

---

### Step 2: Navigate to Backend & Activate Virtual Environment

```bash
cd ~/projects/businesshub-ai/backend
git checkout track/2-db-alembic
source .venv/bin/activate
```

---

### Step 3: Test Alembic Migrations (Upgrade & Downgrade)

1. **Apply all pending migrations to latest (`head`)**:
   ```bash
   alembic upgrade head
   ```
   *Expected Output:*
   ```text
   INFO  [alembic.runtime.migration] Context impl PostgresqlImpl.
   INFO  [alembic.runtime.migration] Will assume transactional DDL.
   INFO  [alembic.runtime.migration] Running upgrade  -> b0ef29871835, initial_schema
   ```

2. **Check Current Migration State**:
   ```bash
   alembic current
   ```
   *Expected Output:* `b0ef29871835 (head)`

3. **Test Rollback/Downgrade to `base`**:
   ```bash
   alembic downgrade base
   ```

4. **Re-apply Migration**:
   ```bash
   alembic upgrade head
   ```

---

### Step 4: Inspect Created Database Tables with PostgreSQL CLI

Query the PostgreSQL container directly to verify table structure:

```bash
docker exec -it businesshub_postgres psql -U postgres -d businesshub_db -c "\dt"
```

**Expected Output:**
```text
               List of relations
 Schema |       Name        | Type  |  Owner   
--------+-------------------+-------+----------
 public | alembic_version   | table | postgres
 public | organizations     | table | postgres
 public | user_roles        | table | postgres
 public | users             | table | postgres
```

Inspect table columns for `organizations` and `users`:
```bash
docker exec -it businesshub_postgres psql -U postgres -d businesshub_db -c "\d organizations"
docker exec -it businesshub_postgres psql -U postgres -d businesshub_db -c "\d users"
docker exec -it businesshub_postgres psql -U postgres -d businesshub_db -c "\d user_roles"
```

---

### Step 5: Run the Automated Pytest Integration Suite

Run pytest to execute all health and migration tests programmatically:

```bash
pytest -v
```

**Expected Output:**
```text
tests/test_database_migrations.py::test_alembic_migrations_upgrade_and_downgrade PASSED [ 50%]
tests/test_health.py::test_healthz_endpoint PASSED                       [100%]

============================== 2 passed in 1.54s ===============================
```
---
**Congratulations on successfully merging Milestone 1 into `develop`!** Having your complete Docker infrastructure active, Pytest running, and your `/api/v1/healthz` endpoint returning a clean `200 OK` is a massive milestone. Your local environment is officially verified and primed for core feature development.

Since your foundation is securely in place, the next logical step in your **Phase 1: Core Platform Foundation** backlog is **Database DDL Schemas, SQLAlchemy Models, and Alembic Initialisation**. Before we can implement multi-tenant authentication, we must establish our base database tables following our strict system contracts.

According to your system architectural specifications, our database design must adhere to these strict constraints:
*   **Database Naming:** Table and column names must be lowercase `snake_case`, and table names must be pluralised (e.g., `organizations`, `users`).
*   **Primary Keys:** Every table must use a UUID v4 as its primary key.
*   **Tenant Isolation:** All tenant-bound tables must feature a mandatory `organization_id` column with a foreign key referencing `organizations(id)` and cascade deletion.
*   **Audit Fields:** Every table must include timezone-aware `created_at` and `updated_at` timestamps.
*   **Soft Deletes:** Business entities must implement a nullable `deleted_at` field rather than hard deleting.

Here is the structured prompt you can copy and paste directly to **Jules / Antigravity** to execute **Milestone 2 (Database Schema & Alembic Migrations)**.

***

### 📋 Copy-Paste Prompt for Jules / Antigravity

```markdown
USER_REQUEST:
We have successfully merged the baseline repository scaffolding (Milestone 1) into 'develop' and have a fully running local Docker suite. 

Your goal for Milestone 2 is to declare our primary SQLAlchemy 2.0 database models and initialize Alembic migrations inside `/backend`.

Please execute the following tasks:

1. GIT BRANCH & WORKFLOW
- Create and switch to a new local feature branch off 'develop' named exactly: track/2-db-alembic
- Follow strict Human-in-the-Loop guidelines: Do NOT attempt to push directly to 'main' or 'develop'. All deliverables must be prepared for a Draft Pull Request.
- All commit messages must follow the Conventional Commits specification (e.g., "feat(db): implement initial models and alembic migration").

2. DATABASE MODEL DECLARATIONS
Following Clean Architecture boundaries (models inside `/backend/app/domain/models/`), declare our core database schemas using Async SQLAlchemy 2.0:
- Create the 'organizations' table:
  * Primary key: UUID v4 (using gen_random_uuid())
  * Fields: name (str), slug (str, unique), subscription_status (str, default 'FREE'), created_at, updated_at
- Create the 'users' table:
  * Primary key: UUID v4
  * Fields: email (str, unique), hashed_password (str), full_name (str), is_active (bool, default True), created_at, updated_at
- Create the 'user_roles' junction table to map Users and Organizations with assigned RBAC permissions:
  * Fields: user_id, organization_id, role (str, representing 'TENANT_OWNER', 'TENANT_ADMIN', etc.)
  * Enforce primary key constraint across (user_id, organization_id) and ON DELETE CASCADE on foreign keys.

Apply global standards to all models:
- Use lowercase snake_case plural naming for tables.
- Standard audit columns (created_at, updated_at with timezone) on all entities.

3. ALEMBIC MIGRATION INITIALIZATION
- Inside `/backend`, initialize Alembic: `alembic init alembic`
- Modify `alembic.ini` and `alembic/env.py` to:
  * Support our async PostgreSQL driver (asyncpg).
  * Load the database connection string from your Pydantic Settings/environment.
  * Import and bind your SQLAlchemy Base metadata to enable auto-generation ('target_metadata').
- Generate the initial migration script: `alembic revision --autogenerate -m "initial_schema"`
- Ensure that the migration script contains explicit names for all primary keys, foreign keys, and indexes.

4. LOCAL VERIFICATION
- Add basic integration tests in `/backend/tests/` that programmatically run `alembic upgrade head` and verify that the 'organizations' and 'users' tables are correctly initialized in the database.
- Confirm both upgrade and downgrade paths function without errors.

When complete, commit your changes to 'track/2-db-alembic' and open a Draft Pull Request for review.
```

***

🔑 Once Jules has initialized the database tables, I can help you design the core multi-tenant security middleware to automatically extract and isolate tenant contexts on every incoming API request.
---
### Summary of Completed Actions

1. **Git Branch & Workflow**:
   - Created and switched to local feature branch: `track/1-foundation-docker` (branched off `develop`).
   - Committed changes adhering strictly to Conventional Commits:
     `feat(foundation): scaffold directory blueprint and backend requirements`

2. **Monorepo Directory Structure**:
   - Scaffolded Clean Architecture directory layout under `backend/app/`:
     - `backend/app/api/` (v1 controllers & routing)
     - `backend/app/core/` (Settings, security, CORS configuration)
     - `backend/app/domain/` (Entities and domain logic)
     - `backend/app/repositories/` (Data access layer for Async SQLAlchemy 2.0)
     - `backend/app/schemas/` (Pydantic v2 DTO schemas)
     - `backend/app/services/` (Application use cases)
     - `backend/tests/` (Pytest test suite)
   - Ensured existing `frontend/` Angular workspace remains untouched.

3. **Backend Initialization & Dependencies**:
   - Verified production-grade `backend/requirements.txt` containing exact package versions:
     - `fastapi==0.111.0`
     - `uvicorn[standard]==0.30.0`
     - `sqlalchemy[asyncio]==2.0.30`
     - `asyncpg==0.29.0`
     - `alembic==1.13.0`
     - `pydantic[email]==2.7.4`
     - `pydantic-settings==2.3.4`
     - `celery==5.4.0`
     - `redis==5.0.4`
     - `structlog==24.1.0`
     - `python-multipart==0.0.9`

4. **FastAPI Entry Point & Configuration**:
   - Scaffolded `backend/app/main.py` with CORS middleware and API v1 routing.
   - Implemented `/api/v1/healthz` health check endpoint returning `200 OK`.
   - Updated `backend/app/core/config.py` using Pydantic Settings to automatically parse `.env` values (PostgreSQL, Redis, MinIO, Celery, JWT, Logging).

5. **Verification & Documentation**:
   - Created `backend/README.md` covering environment setup, dependency installation, and running `uvicorn app.main:app --reload`.
   - Ran `pytest` suite locally in WSL to verify syntax and health check functionality (`1 passed in 1.41s`).
   - Root infrastructure files (`docker-compose.yml`, `.env`, `env.example`) remain unmodified.

### Deliverable Ready for Review
The branch `track/1-foundation-docker` is clean and ready for a Draft Pull Request against `develop`.
---
This is a pristine, production-ready starting point. With your local WSL subsystem networking resolved, your PostgreSQL (with pgvector), Redis, and MinIO containers fully operational, and your Angular development server running cleanly on port 4200, the baseline workspace is completely verified.

You are in a perfect position to launch **Milestone 1 (Repository Scaffolding & Backend Initialization)**. 

Since you have cleaned up your temporary tracking branches, here is the exact, updated prompt to feed to **Jules / Antigravity** to execute this phase. This prompt is explicitly updated to instruct the agent to respect your working container configurations and existing local environment setup.

***

### 📋 Copy-Paste Prompt for Jules / Antigravity

```markdown
USER_REQUEST:
We are bootstrapping the "BusinessHub AI" SaaS platform. The local development infrastructure is already up and fully verified on WSL:
- PostgreSQL 16 + pgvector (via pgvector/pgvector:0.7.4-pg16) is active on port 5432.
- Redis 7-alpine is active on port 6379.
- MinIO object storage is active with an automated initialization job.
- An Angular development workspace is serving locally on port 4200.

The baseline `docker-compose.yml` and `.env`/`env.example` configurations are finalized. Do NOT overwrite, modify, or break these root-level infrastructure files.

Your goal is to scaffold the clean monorepo directory blueprint and bootstrap the initial backend configuration.

Please execute the following tasks:

1. GIT BRANCH & WORKFLOW
- Create and switch to a new local feature branch off 'develop' named exactly: track/1-foundation-docker
- Follow strict Human-in-the-Loop guidelines: Do NOT attempt to push directly to 'main' or 'develop'. All deliverables must be prepared for a Draft Pull Request.
- All commit messages must strictly follow the Conventional Commits specification (e.g., "feat(foundation): scaffold directory blueprint and backend requirements").

2. MONOREPO STRUCTURE
Scaffold the following directory structure in the repository root:
├── backend/                  # Python 3.12+ / FastAPI backend
│   ├── app/                  # Clean Architecture source code directory
│   │   ├── api/              # API controllers and routing layer
│   │   ├── core/             # Settings, security, and middleware
│   │   ├── domain/           # Business entities and domain logic
│   │   ├── repositories/     # Data access layer (Async SQLAlchemy 2.0)
│   │   ├── schemas/          # Pydantic v2 DTO schemas
│   │   └── services/         # Application business use-cases
│   └── tests/                # Pytest suite directory
└── frontend/                 # Angular frontend workspace (leave existing workspace untouched)

3. BACKEND INITIALIZATION & DEPENDENCIES
In `/backend`, create a production-grade `requirements.txt` containing the exact, un-hallucinated stack packages and versions:
- fastapi==0.111.0 (ASGI framework)
- uvicorn[standard]==0.30.0 (ASGI server)
- sqlalchemy[asyncio]==2.0.30 (ORM)
- asyncpg==0.29.0 (PostgreSQL async driver)
- alembic==1.13.0 (Database migrations)
- pydantic[email]==2.7.4 (Data validation & settings management)
- pydantic-settings==2.3.4 (Environment configuration parsing)
- celery==5.4.0 (Asynchronous task queue)
- redis==5.0.4 (Cache/Queue client library)
- structlog==24.1.0 (Structured logging)
- python-multipart==0.0.9 (Form parsing support)

4. BASIC BACKEND ENTRY POINT
- Inside `/backend/app/main.py`, scaffold a minimal FastAPI application.
- Implement a basic `/api/v1/healthz` endpoint that returns a placeholder 200 OK status.
- Configure Pydantic Settings in `/backend/app/core/config.py` to parse configuration parameters from the root `.env` file, prioritizing database, redis, and minio connection strings required for startup.

5. VERIFICATION
- Write a short `backend/README.md` explaining how to create a virtual environment (`python -m venv .venv`), install the dependencies, and start the FastAPI server locally (`uvicorn app.main:app --reload`).

When completed, verify all code files pass basic syntax checks, commit your work to `track/1-foundation-docker`, and submit a Draft Pull Request for architectural review.
```

***

🚀 Let me know when Jules has prepared the Draft PR, and we can run a deep architectural review of the clean backend entry point and settings parsing!
---
That AI prompt is a **highly structured set of technical instructions** designed to be given directly to your AI coding agent, **Jules** (or **Antigravity**). 

Because we spent quite a bit of effort getting your WSL network bridge fixed and your Docker containers running smoothly, **this prompt acts as a safety guard and instruction manual** for the coding agent. It tells the agent exactly how to build the initial codebase without breaking the working local setup you just verified.

Here is a quick breakdown of what the prompt instructs the agent to do:

### 1. **Keep Your Docker Infrastructure Safe**
The prompt explicitly warns the agent: *"Do NOT overwrite, modify, or break these root-level infrastructure files."* This ensures that Jules respects your working `docker-compose.yml` and `.env` settings, avoiding any risk of re-introducing the network errors we just resolved.

### 2. **Establish the Git Workflow**
It forces Jules to operate under strict enterprise standards:
*   Creating a clean feature branch named **`track/1-foundation-docker`** off `develop`.
*   Working entirely in a **Draft Pull Request** for you to review, rather than committing directly to your primary branches.
*   Writing structured commit messages following the **Conventional Commits** standard.

### 3. **Scaffold the Clean Architecture Folders**
It directs Jules to lay out the standardized **Modular Monolith folder structure** in the `/backend` directory according to **Clean Architecture** principles:
*   `api/` (for REST routes).
*   `core/` (for security, middleware, and database configs).
*   `domain/` (for core business models).
*   `repositories/` & `services/` (for database access and application logic).

### 4. **Initialize Backend Dependencies & Entry Point**
It commands the agent to bootstrap the Python environment:
*   Write a `requirements.txt` with the exact, un-hallucinated packages for your async stack (FastAPI, SQLAlchemy 2.0, Pydantic v2, Celery, and Redis).
*   Create `main.py` with a basic **`/api/v1/healthz`** monitoring endpoint.
*   Configure configuration loading via Pydantic Settings so the backend can automatically bind to your running PostgreSQL and Redis containers.

---

### **How to use it:**
Simply copy the entire prompt block from the previous message and paste it into your interface with **Jules/Antigravity** (in GitHub, Cursor, or whatever workspace environment you are using). It gives the agent everything it needs to successfully deliver **Milestone 1** of BusinessHub AI!

🤖 Once you hand this off to Jules, let me know if you would like me to review the generated code or explain any of the architectural choices they make in the Draft PR!
