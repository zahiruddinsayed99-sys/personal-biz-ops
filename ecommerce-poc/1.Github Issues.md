Here is the complete breakdown of tasks formatted as ready-to-use GitHub issues, tailored to your Angular (v16+) and FastAPI tech stack.

---

### Issue #1: [Bug] Fix Product Image Rendering & Asset Pipeline

**Labels:** `bug`, `frontend`, `backend`, `milestone-1`

#### Description

Product cards on the catalog page (`/`) and product detail page (`/product/:id`) render gray placeholder blocks instead of actual images.

#### Technical Specifications

* **Backend (FastAPI):** Ensure the mock product model includes valid `image_url` fields and that static image assets are properly mounted and served via `fastapi.staticfiles`.
* **Frontend (Angular):** Update product card and detail templates with standard `<img>` tags, binding to `product.imageUrl`. Add graceful image fallback handling via `(error)` directive.

#### Acceptance Criteria

* [ ] Catalog page renders distinct, properly proportioned product images inside each card.
* [ ] Product detail page displays the hero product image without stretching or distortion.
* [ ] A fallback placeholder image is shown if an image fails to load.

---

### Issue #2: [Feature] Implement Reactive Cart Service & Fix "Add to Cart" Action

**Labels:** `bug`, `state-management`, `frontend`, `milestone-1`

#### Description

The "Add to Cart" button on catalog cards is unresponsive. A unified reactive state management service is needed to manage cart items and calculations.

#### Technical Specifications

* **State Management:** Implement `CartService` using RxJS `BehaviorSubject` or Angular Signals (`signal`, `computed`).
* **State Model:** Track item ID, title, quantity, price, image, and optional kit custom selections.
* **Component Wire-up:** Connect the catalog card CTA to `CartService.addToCart(product)`.
* **Feedback:** Trigger an Angular Material Snackbar / Toast or visual feedback indicating success.

#### Acceptance Criteria

* [ ] Clicking "Add to Cart" on any catalog item increments the cart count and adds the item to the reactive cart state.
* [ ] Adding an existing item increases its quantity instead of creating duplicates.
* [ ] A snackbar/toast confirms item addition to the user.

---

### Issue #3: [Feature] Add Global Cart Header Navigation & Clean Public Nav

**Labels:** `enhancement`, `frontend`, `ui/ux`, `milestone-1`

#### Description

The header lacks a direct link to the cart. The "Admin" link is currently exposed directly to standard shoppers.

#### Technical Specifications

* **Angular Component:** Update `HeaderComponent` to include a Cart icon/link (`routerLink="/cart"`).
* **Reactive Badge:** Bind an Angular Material badge (`matBadge`) or Tailwind badge to `CartService.totalItems$` to display dynamic item counts.
* **Navigation Cleanup:** Remove the public "Admin" link from the main user header (reserve access to direct route `/admin`).

#### Acceptance Criteria

* [ ] Cart icon is visible on all routes in the top header.
* [ ] Badge displays the exact total item count and updates immediately when items are added.
* [ ] Clicking the cart icon routes to `/cart`.
* [ ] "Admin" link is removed from the primary shopper view.

---

### Issue #4: [Feature] Cart Page Management & Currency Formatting

**Labels:** `enhancement`, `frontend`, `ui/ux`, `milestone-1`

#### Description

The `/cart` page currently renders static-like rows with no ability to remove items, alter quantities, or inspect line items easily.

#### Technical Specifications

* **Interactive Controls:** Add `+` / `-` quantity buttons and a delete/trash icon for each line item.
* **UI Details:** Render product thumbnails next to titles and make titles clickable (`routerLink="/product/:id"`).
* **Currency Formatting:** Apply Angular custom pipe or `Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR' })` to enforce standard Indian numbering (e.g., `₹1,499`).

#### Acceptance Criteria

* [ ] Users can increase, decrease, or remove items directly from `/cart`.
* [ ] Cart subtotals, voucher deductions, and final totals update reactively.
* [ ] Prices throughout the cart display properly in INR format (`₹`).
* [ ] Cart renders an empty-state illustration and "Start Shopping" button when empty.

---

### Issue #5: [Feature] Enforce Custom Vanity Kit Builder Rules & Validation

**Labels:** `enhancement`, `frontend`, `milestone-2`

#### Description

The custom kit builder on `/product/:id` must enforce strict selection constraints (select exactly 3 variations) and pass the configuration to the cart.

#### Technical Specifications

* **Reactive Logic:** Use Angular Reactive Forms or Signals to monitor checkbox selections.
* **Button State:** Keep the "Add Custom Kit to Cart" button visible at all times, styled with `disabled` states until `selectedCount === 3`.
* **Payload Structure:** On submit, pass the parent product along with the array of 3 selected variant names to `CartService.addCustomKit()`.

#### Acceptance Criteria

* [ ] Checkboxes prevent selecting more than 3 variations.
* [ ] "Add Custom Kit to Cart" CTA remains disabled with visual guidance until exactly 3 items are checked.
* [ ] Added kit appears in `/cart` with its custom selections clearly itemized.

---

### Issue #6: [Feature] Checkout Form Validation & Mock Order Creation

**Labels:** `enhancement`, `frontend`, `backend`, `milestone-2`

#### Description

Ensure structured shipping inputs, validate contact info, simulate Razorpay payment, and register orders in the FastAPI in-memory store.

#### Technical Specifications

* **Frontend (Angular):**
* Convert the single address text box into granular fields (`Street`, `Pincode`, `City`, `State`).
* Add validators for email format and 6-digit Indian PIN codes.
* Trigger mock Razorpay payment modal on checkout.


* **Backend (FastAPI):**
* Create `POST /api/orders` to store order payload in an in-memory dictionary/list.
* Return generated `order_id` (e.g., `ord_5437f4bb`) and timestamp.


* **Confirmation:**
* Route to `/order-confirmation?orderId=...` and add a "Continue Shopping" CTA.



#### Acceptance Criteria

* [ ] Form blocks submission and highlights invalid email or incomplete shipping fields.
* [ ] Successful simulated payment creates an in-memory order record via FastAPI.
* [ ] User is routed to order confirmation showing their unique `orderId` and a functional "Continue Shopping" button.

---

### Issue #7: [Feature] Admin Order Actions, Confirmation Dialogs & Timestamps

**Labels:** `enhancement`, `admin`, `frontend`, `backend`, `milestone-3`

#### Description

Protect the `/admin` dashboard against accidental order cancellations or status changes and add timestamps for tracking.

#### Technical Specifications

* **Frontend:**
* Add a Date/Time column formatted in IST.
* Integrate an Angular Material Dialog (`MatDialog`) prompting "Are you sure you want to change this order status?" before firing action mutations.


* **Backend (FastAPI):**
* Implement `PATCH /api/orders/{order_id}/status` to toggle between `Placed`, `Delivered`, and `Cancelled`.
* Return updated order records to refresh the table.



#### Acceptance Criteria

* [ ] Orders table displays an accurate timestamp for each transaction.
* [ ] Clicking "Mark Delivered" or "Cancel" opens a modal confirmation dialog.
* [ ] Confirmed status updates reflect immediately in the admin table.

---
Here is the GitHub issue detailing the theme and color selection features. I've placed this in Milestone 3, as it's a great UX polish feature to tackle once the core checkout flows are unblocked.

---

### Issue #8: [Feature] Implement Dark/Light Mode & Theme Color Selection

**Labels:** `enhancement`, `ui/ux`, `frontend`, `milestone-3`

#### Description

Allow users to personalize their shopping experience by toggling between dark and light modes, and selecting from a small palette of primary accent colors. The app should remember these preferences across sessions.

#### Technical Specifications

* **Tailwind Configuration:**
* Update `tailwind.config.js` to enable class-based dark mode (`darkMode: 'class'`).
* Define primary color utilities using CSS variables (e.g., `--color-primary`) so they can be swapped dynamically, rather than hardcoding specific Tailwind color classes (like `bg-pink-600`).


* **State Management (`ThemeService`):**
* Create an Angular service using `BehaviorSubject` or Signals to track the current mode (`light` | `dark`) and current theme color (e.g., `pink` | `violet` | `teal`).
* Sync these values with browser `localStorage` on initialization and on every change.


* **Angular Material Integration:**
* Ensure the `.dark-theme` class is applied to the root `<body>` or `<app-root>` element so Angular Material components (like the dialogs and snackbars) adjust their backgrounds and text colors accordingly.


* **UI Implementation:**
* Add a visual dropdown or icon group in the `HeaderComponent` (near the cart) containing a Sun/Moon toggle for dark mode and small colored circles for the theme selection.



#### Acceptance Criteria

* [ ] Header includes a visible toggle for Dark/Light mode and options for at least 3 primary brand colors (e.g., Default Pink, Violet, Teal).
* [ ] Toggling dark mode seamlessly updates the background, text, product cards, and checkout surfaces without rendering issues.
* [ ] Selecting a new theme color updates the primary buttons (like "Add to Cart"), active states, and header accents.
* [ ] Refreshing or closing and reopening the browser retains the user's selected mode and color preferences.
