Here is a strategic way to divide the 8 issues, setting up one workflow to focus primarily on Angular UI and presentation, while the other handles the FastAPI backend integration and full-stack logic.

### 🎨 Focus Area A: Frontend & UI State (Suggested for Jules)

Assign these tasks to the agent managing your primary Angular component updates, pull requests, and user interface logic.

* **Issue #1: Fix Product Image Rendering:** Requires direct HTML template and Tailwind utility updates.
* **Issue #3: Global Cart Header Navigation:** Strictly Angular UI work involving components, routing, and Material badges.
* **Issue #5: Enforce Custom Vanity Kit Builder Rules:** Focuses heavily on Angular Reactive Forms or Signals to strictly control user selection limits.
* **Issue #8: Implement Dark/Light Mode:** Pure frontend polish involving `tailwind.config.js` and DOM class manipulation.

### ⚙️ Focus Area B: Core Logic & API Integrations (Suggested for Antigravity)

Assign these tasks to the platform handling your broader agentic workflows, API endpoint mockups, and foundational state services.

* **Issue #2: Reactive Cart Service:** Establishing the core RxJS/Signal state architecture is foundational and dictates how the rest of the app communicates.
* **Issue #4: Cart Page Management:** Builds directly on the `CartService` logic to implement interactive UI controls.
* **Issue #6: Checkout Form & Mock Order Creation:** Requires building the FastAPI `POST /api/orders` endpoint alongside the Angular form submission.
* **Issue #7: Admin Order Actions:** Needs the FastAPI `PATCH` endpoint for status changes and the corresponding frontend API calls.

This division keeps the Angular UI templates relatively isolated from the FastAPI routing and core RxJS logic, minimizing merge conflicts when automated pull requests are opened. While this split covers a standard vertical slice, exploring alternative configurations—such as having one agent write feature code while the other strictly handles automated testing and CI/CD pipelines—can also be highly effective.

How have you configured Jules and Antigravity's specific operational modes or technical scopes for this particular repository?
